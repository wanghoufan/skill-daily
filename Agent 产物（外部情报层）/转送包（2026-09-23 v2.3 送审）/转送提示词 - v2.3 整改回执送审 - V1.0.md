# 转送提示词 —— External Skill Intelligence v2.3 整改回执送审

> 生成：2026-09-23 · WorkBuddy（MacBook）
> 用法：整段复制，连材料一起发给审查者。

---

你上一轮交付了《External Skill Intelligence v2.3 语义精度最终整改提示词 V1.1》（共 12 节）。现已按其要求完成全部整改，请复审。

## 一、材料

- 文件名：`转送审查者-v5-外部情报层-2026-09-22.zip`
- 大小 / 校验：496,367 B（0.47 MB）· 24 个文件
  SHA-256 `43e7aa30f3f48cb3b898fb5fea1c54b11123d21db9c8d7a82cc5b60b913b731d`
- 解压后顶层文件夹：`转送审查者 v5 材料（外部情报层）/`

解开后能看到什么：

| 文件 | 看什么 |
|---|---|
| `转送说明.md` | 给审查者的导读（12 节），建议先看 |
| `README.md` | 技术说明；§6.3 是 §二 的整改记录，§6.4 是 §五 的整改记录 |
| `CANDIDATES_DIGEST.md` | 精简对照件；§五 是 v2.3 整改记录表，§2.1 是功能族 ALTERNATIVES 表 |
| `EXTERNAL_SKILLS_CONTEXT.md` | 日报底座；`PERSONALIZED_TOP30` 机器块含 `min_score` / `functional_duplicates` / `t3_uncorroborated` |
| `SKILL_CANDIDATES.json` | 全量候选池（任何数字都可自行复算） |
| `SKILL_SOURCE_REGISTRY.json` | 来源注册表 |
| `支撑/` | scripts 10 个 + `config/scoring.json` + `tests/test_pipeline.py` + 输入 2 个 |
| `raw/` | 当日原始发现快照 4 个 |

## 二、v2.3 逐节落地情况

| 节 | 你的要求 | 落地方式 |
|---|---|---|
| §一 / §六 | PROJECT MATCH 禁止「类型适用」单独造匹配 | 新增 `DIRECT_PROJECT_EVIDENCE`：`tech_stack` / `positioning` / `shared_need` / `lexical` 四类**至少一类成立**才产生项目匹配，否则 `matched_projects=[]`；`TYPE_W` 30 → 10，降为 secondary boost；`lexical` 排除平台泛词（macos / windows / android / ios / python…），禁止子串匹配 |
| §二 | 继续收紧 4 条 NEED_RULE | `llm-api`：LLM 语境 AND API/SDK/tool-calling 动作 + `none_of` 广告类词；`deploy`：只留真部署动词，Vercel / CI-CD 降为 `context_terms`；`voice-input`：去掉裸 `dictation` / `transcription`；`supabase-db`：只认明确 `supabase` |
| §三 / §八 | Top 跨仓功能簇折叠 | 新增 `functional_family()`（normalized name 相同且 ≥5 字符 / description jaccard ≥0.62 且名称同前缀 / content fingerprint 相同）；Top 只出 PRIMARY，其余进 `### 2.1 ALTERNATIVES`，**不会被无声丢弃** |
| §四 | always_block 误伤修正 | `_should_block(rule, ctx) = rule ∈ BLOCK_ELIGIBLE_RULES 且 ctx ≠ "warning"`；新增 `warning` 语境识别（`never` / `do not` / `禁止` / `不要` / 举反例 / 安全审计）；收窄 `destructive_rm_root` 正则（不再把 `/tmp/xxx` 当根目录） |
| §五 | Top 三道质量门，不凑数 | `min_score=55` + 必须有真实个性化证据（matched need / capability gap / update / replacement 至少一项）+ T3·`discovery_only` 需 T1·T2·官方交叉佐证才可进 Top；不足 30 直接输出 TopN |
| §九 | 归档包自定位 + 摘要三分 | 输入 A 三级优先序：`SKILL_CONTEXT_PATH` → 包内 `支撑/输入/SKILL_CONTEXT.md` → 本机默认；输入 B：env → 同级 v4 自动发现（向上探 3 级）→ 找不到则 SKIP 并提示；`PATH_RESOLUTION` 记录命中来源；测试摘要分 `pass` / `skip` / `fail`，**SKIP 不计入 PASS** |
| §十 | 新增回归测试 | §十 的 15 个回归点全覆盖；测试项 21 → 27 |
| §十一 | 重新生成 v2.3 基线 | registry / candidates / context / digest / README 全部按 v2.3 口径重生成，旧数字未保留 |

## 三、我方自测验收摘要（§十二 规定格式）

```text
MATCHING:
false_positive_regression: PASS
project_match_direct_evidence_rate: 351/351
matched_projects_coverage: 86/173

TOP:
target_limit: 30
actual_count: 22
functional_duplicates: 1
t3_uncorroborated: 23
min_score: 55

SECURITY:
false_block_regression: PASS
block: 3
review_required: 202
pass: 395
unscanned: 505

TESTS:
pass: 27
skip: 0
fail: 0

BLOCKERS:
none
```

## 四、请你复核这四点

1. 上表「v2.3 逐节落地」是否真的落实 —— 请直接看代码与数据，不要只看自述。
2. §十二 每个数字能否自证 —— 数据源在 `SKILL_CANDIDATES.json` 与 `EXTERNAL_SKILLS_CONTEXT.md` 的 `PERSONALIZED_TOP30` 机器块。
3. 归档包自定位：解压后直接跑 `python3 支撑/tests/test_pipeline.py`。**只有本包**（无同级 v4 文件夹）时预期 `pass=26 skip=1 fail=0`（输入 A 自动命中包内文件，输入 B 找不到 → 1 项 SKIP 并打印原因）；把 `转送审查者 v4 材料（Canonical 模型）` 放到同级后再跑，预期 `pass=27 skip=0 fail=0`。
4. 对照件可复现：`python3 支撑/scripts/make_digest.py -o /tmp/x.md`，除「自定位来源」那一行外，应与包内 `CANDIDATES_DIGEST.md` 逐位一致。

## 五、输出要求

只回：一段结论（通过 / 不通过 + 理由）+ 如有不符合处，按同一格式给出下一轮整改提示词。不要复述本提示词。
