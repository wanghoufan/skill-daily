# 转送提示词 —— External Skill Intelligence v2.5 整改回执送审（冻结前最后一轮）

> 生成：2026-09-23 · WorkBuddy（MacBook Air）
> 用法：整段复制，连材料一起发给审查者。

---

你上一轮交付了《External Skill Intelligence v2.5 冻结前最后整改提示词 V1.1》（共 14 节）。现已按其要求完成整改，请复审。

**本轮范围声明**：v2.4 已验收通过的技术栈 STRONG/SECONDARY 分级、强证据不变式、deploy 二次收紧、testing_qa 主能力、domain_mismatch 生效、Top 四道质量门、Canonical 体系、安全 Gate、归档自定位**全部未重做、未回退**（§十二）。本轮只修你列出的剩余语义误判：**android 假需求 / 逗号枚举否定漏判 / 词面证据三处假相关 / mcp_dev / PPTX / github-auto / 四态证据分级**。全程**未新增任何 blacklist**，修的都是通用证据规则（§十一）。

## 一、材料

- 文件名：`转送审查者-v5-外部情报层-2026-09-22.zip`
- 大小 / 校验：**529,366 B（0.51 MB）· 24 个文件**
  SHA-256 `f145e576a3881d8e596807c761525c7aa25ba5690c4dce315beee0bcb7aab9a8`
- 中文名 UTF-8 标志位 **24/24** 齐备（Python zipfile 打包，已实测复核）
- 解压后顶层文件夹：`转送审查者 v5 材料（外部情报层）/`
- 上一包（v2.4）已改名为 `…-v2.4-旧包.zip`，本包为**替换件**，请以本包为准。
- **归档包端到端已实测**：解压到与主仓无关的隔离目录后直接跑 `python3 支撑/tests/test_pipeline.py` →
  `pass=42 skip=1 fail=0`（43 项；输入 B 缺失那 1 项 SKIP 并打印原因，不伪装 PASS）；
  把 v4 文件夹放同级后 `pass=43 skip=0 fail=0`；`make_digest.py` 重生成与包内件**只有 1 行不同**（刻意随环境变化的输入 A 自定位行），其余逐位一致。

解开后能看到什么：

| 文件 | 看什么 |
|---|---|
| `转送说明.md` | 给审查者的导读（9 节），建议先看；**§四** 是 v2.5 基线数字，**§五** 是本轮 8 项收口对照表 |
| `README.md` | 技术说明；**§6.3.3** 是 v2.5 §一–§九 的落地记录（a–e），**§3.1** 含 4 个新计数（2 个恒 0 哨兵） |
| `CANDIDATES_DIGEST.md` | 精简对照件；**§五** 是 v2.5 整改记录表（含 5.1/5.2），**§一** 是 `PROJECT_MATCH_QUALITY` 分项表，**§十一** 是**按包内真实数据现算的 Top 重审块** |
| `EXTERNAL_SKILLS_CONTEXT.md` | 日报底座；含 `PROJECT_MATCH_QUALITY` 机器块与 `PERSONALIZED_TOP30` 自证字段 |
| `SKILL_CANDIDATES.json` | 全量候选池 1,106 条（**version 5**，任何数字都可自行复算）；每候选新增 `capability_evidence_context` 四态字段 |
| `SKILL_SOURCE_REGISTRY.json` | 来源注册表 17 源（未变） |
| `支撑/` | scripts 10 个 + `config/scoring.json`（**version 保持 4：本轮未动任何权重**）+ `tests/test_pipeline.py`（**43 项**）+ 输入 2 个 |
| `raw/` | 当日原始发现快照 4 个 |

## 二、v2.5 逐节落地情况

| 节 | 你的要求 | 落地方式 | 可自查点 |
|---|---|---|---|
| §一 | android need = 平台 AND 真实 QA/构建验收证据 | `NEED_RULE["android"]` 改 custom（`_cls_android`）：A/B 两级证据列表，裸 `device / build / install / launch` 不成立 | `google-mobile-ads-get-started` / `-validate` 的 `matched_needs` **无 android**；`orca-emulator-android` 仍得 android + mobile_qa（79.4，Top #1，反向保护） |
| §二 | 否定窗口在逗号枚举中不漏判 | **两段式**：`_positive_chunks()` 先按句点（后须跟空白/行尾，护住 `next.js`/`.net`/`node.js`）、换行、分号、句末符号切**段**——**逗号不再是分句边界**；段内按 `[_COMMA_SPLIT_RE]` 行走**否定状态机**，否定沿逗号传播，仅显式对比词（`_POS_RESTORE_RE`：but / however / instead / whereas / use for / 但是 / 不过 / 而是 / 可用于 / 即可…）恢复正向；先查否定、再查恢复；判定只在正向视图做，Skill 名不受描述否定影响 | `Don't use for A, B, or C` → A/B/C 全不成立；`Not for A, but use for B` → A 否 B 正；`agent-platform-prompt-management` / `-tuning` 不再命中 deploy / llm-api。`negated_evidence_rejected` 30 → **118**（计数语义变化，见下表注） |
| §三 | 技术泛词不得充当词面直接证据 | `_LEXICAL_TECH_BAN`：凡在 `TECH_MATCH` 词表内的术语一律禁入词面直接证据，违规计入哨兵 `tech_words_used_as_lexical_direct_evidence`（**必须恒 0**） | `react-view-transitions` 不再凭 react/native 匹配 React Native 项目；包内哨兵值 = **0** |
| §四 | 重构 lexical high-signal：裸 prompt 出局，prompt 类项目走 phrase/domain evidence | 裸 `prompt` 移出可独立成立词集（哨兵 `bare_prompt_direct_evidence` **恒 0**）；新增 `_PROJECT_DOMAIN_PHRASES`（curated 域短语，`prompt management` / `prompt library` / `family insurance`…），仅**短语级**可独立成立并计入 `lexical_phrase_direct_evidence` | `claude-api`、`breakdown-test` 均不再匹配 `prompt-manager`；`agent-platform-prompt-management` 凭短语 `prompt management` 合法成立（唯一 1 条 lexical，score 28，watch，不进 Top） |
| §五 | lexical evidence 只描述核心任务，触发说明里的偶然词不算；词面重叠降级 | 普通词面重叠**不再单独产生 matched_projects**：有其它直接证据时只作「词面重叠（次要加分）」，无则计入 `lexical_alone_rejected` | `lexical_evidence` 49 → **1**（就是上面那条短语）；`secondary_tech_only_rejected` 106 |
| §六 | mcp_dev = 核心 MCP 开发能力 | `_cls_mcp`：build/create/implement… + mcp 50 字符内同现（`_MCP_DEV_RE`）或名称含 MCP server 短语 → primary；仅「使用/调用 MCP」→ supporting（×0.5 权重） | `claude-api` 不得 mcp_dev；`mcp-builder` 保留 primary（反向保护） |
| §七 | docx_xlsx 不得被 PPTX 单独满足 | `_cls_docx` 只认 Word/Excel；PPTX 归**新独立标签 `pptx_processing`** | `publish-to-pages`（PPTX）得 `pptx_processing`、不再得 docx_xlsx；DOCX/XLSX/Excel 候选仍命中 docx_xlsx（反向保护） |
| §八 | github-auto 不能因裸 GitHub 命中 | `GITHUB_AUTO_RULE = all_groups[平台词组 AND 运维/自动化语义组]`（release/branch/issue/PR/automation…） | `breakdown-test` 不再命中 github-auto；真 GitHub issue/PR/Actions 自动化候选仍命中（反向保护） |
| §九 | 候选级「核心能力 vs 提及」统一 | 新增四态字段 **`capability_evidence_context`** ∈ {primary / supporting / mention / negated}（`EVIDENCE_CLASSIFIERS` + `evidence_context()`，每条候选输出）；`analyze.py`：**只有 primary 拿满 gap 加分**，supporting 封顶 **8** 且对应 need 权重 **×0.5**，mention/negated 封顶 **2**；`matched_needs` 逐条带 `evidence_level` 可审计 | 测试用合成候选断言 `scores.capability_gap` 20 / 8 / 2 三档；JSON 里每候选可查 `capability_evidence_context` |
| §十 | 新增 18 项回归测试 | 新增 **8 个测试函数**逐项覆盖你的 18 个回归点：`test_v25_android_real_qa_evidence`（1,2）/ `test_v25_negation_comma_enumeration`（3,4,5,6,18）/ `test_v25_lexical_evidence_tightened`（7,8,9,10,11）/ `test_v25_mcp_dev_core_capability`（12,13）/ `test_v25_docx_xlsx_vs_pptx`（14,15）/ `test_v25_github_auto_needs_ops_semantics`（16,17）/ `test_v25_capability_evidence_context`（§九三档）/ `test_v25_top_reaudit_final_data`（§十一数据级断言）。测试项 **35 → 43** | `pass=43 skip=0 fail=0`（本机）；隔离解包 `pass=42 skip=1 fail=0` |
| §十一 | 重审 Top：修规则不拉黑 | **未新增任何 blacklist**。`test_v25_top_reaudit_final_data` 直接对包内数据断言：3 组禁选项目匹配 0 条、android/deploy 假需求 0 条、两哨兵 0、Top 重算无未解除错配；对照件 §十一 为现算块 | Top 20 → **19**（`actual_count` 自证字段可查）；`google_mobile_ads_false_top: no`；`agent_platform_prompt_management` 仍在池（watch 55.0）但不在 Top、needs 为空 |
| §十二 | 不回退 v2.4 | 原 **35 项全部保留且全过**（含中文死词自检、逗号分句修正的 v2.4 用例——本轮按 §二 把「逗号是分句边界」改回「逗号在段内传播否定」，v2.4 的 B 缺陷修正式被 §二的更正确实现取代，语义结果与你 §二 示例一致，该变化在 README §6.3.2(a)/§6.3.3(b) 有记录）；Docker container / testing_qa / mismatch 门 / 安全 Gate / 折叠 / 自定位 / 三分摘要全部保持 | `CANDIDATES_DIGEST.md` §5.2 的「不重做」表 + 43/43 |
| §十三 | 重新生成 v2.5 全部交付物 | registry / candidates(v5) / context / digest / README / tests 全部按 v2.5 口径重出，未保留旧数字 | 本包即重打包件 |

## 三、我方自测验收摘要（§十四 规定格式，交付前现跑）

```text
NEED_MATCH:
android_qa_false_positive_regression: PASS
negation_list_regression: PASS
github_auto_false_positive_regression: PASS

PROJECT_MATCH:
lexical_false_positive_regression: PASS
bare_prompt_direct_evidence: 0
tech_words_used_as_lexical_direct_evidence: 0

CAPABILITY_TAGS:
mcp_dev_false_positive_regression: PASS
docx_xlsx_false_positive_regression: PASS

TOP:
target_limit: 30
actual_count: 19
google_mobile_ads_false_top: no
agent_platform_prompt_management_false_deploy: no
contains_ignore_or_reject: no
unresolved_domain_mismatch: 0

SECURITY:
regression: PASS

TESTS:
pass: 43
skip: 0
fail: 0

BLOCKERS:
none
```

（补充口径：候选池 1,106 / raw 1,170；install_candidate **15** / watch 492 / ignore 596 / reject 3；有需求证据候选 **178**（v2.4 为 217）；matched_projects 覆盖率 **16%**（18/112）、直接证据完整性 **122/122**；PROJECT_MATCH_QUALITY：matched_candidates 66 / strong_tech 103 / positioning 12 / shared_need 20 / lexical **1** / secondary_tech_only_rejected 106 / negated_evidence_rejected **118** / lexical_single_rejected 943 / lexical_alone_rejected 1 / lexical_phrase_direct_evidence 1；`t3_uncorroborated` 23 / `mismatch_blocked_from_top` 2 / `functional_duplicates` 1；安全 pass 395 / review_required 202 / block 3 / unscanned 506——**安全层本轮零改动**。）

## 四、请你复核这五点

1. **§二 否定窗口是否是通用实现而非补丁**（`match_rules.py` → `_positive_chunks` / `_POS_RESTORE_RE` / `hit_pos`）。
   建议自造句子验三件事：① `Don't use for A, B, or C` 全列否定；② `Not for A, but use for B` 只否 A；
   ③ `Next.js App Router patterns.` 不被切碎。并核对 `agent-platform-prompt-management`：
   它**仍在池里**（watch），被修的只是 deploy 假需求与裸 prompt 假匹配——**没有拉黑任何候选**。
2. **两个恒 0 哨兵是否真的在守卫**（`QUALITY.bare_prompt_direct_evidence` / `tech_words_used_as_lexical_direct_evidence`）。
   这两个计数只有在违规代码路径里才会 ++；请抽查 `match_projects` ④ 的分支，并复跑
   `test_v25_lexical_evidence_tightened` 与 `test_v24_lexical_stoplist`（v2.4 用例未回退）。
3. **§九 四态是否贯穿计分而非只记录**（`analyze.py`）：抽一条 supporting 候选复算——
   其 need 贡献应为 ×0.5、gap 加分应 ≤8；mention/negated 应 ≤2。
   对照：`mcp-builder`（primary，满权重）vs 仅「使用 MCP」的候选（supporting/mention）。
4. **反向保护**：本轮收口容易、收死难——请验 `orca-emulator-android`（android+mobile_qa，Top #1）、
   `webapp-testing` / `e2e-testing-patterns`（testing_qa）、真 DOCX/XLSX 候选（docx_xlsx）、
   真 GitHub 自动化候选（github-auto）、`mcp-builder` 五条**都没有被误杀**。
5. **Top 与质量门无回退**（`build_context.py` → `select_top` 四道门 + `test_v25_top_reaudit_final_data`
   对包内数据的 5 组断言）；另核对 `CANDIDATES_DIGEST.md` §十一 的现算 Top 重审块与 `EXTERNAL_SKILLS_CONTEXT.md`
   自证字段一致（`actual_count: 19`、`top_candidates_with_unresolved_mismatch: 0`）。

## 五、输出要求

只回：一段结论（通过 / 不通过 + 理由）+ 如有不符合处，按同一格式给出下一轮整改提示词。不要复述本提示词。
