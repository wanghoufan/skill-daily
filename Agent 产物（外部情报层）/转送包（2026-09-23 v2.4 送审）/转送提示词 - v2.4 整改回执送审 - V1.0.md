# 转送提示词 —— External Skill Intelligence v2.4 整改回执送审

> 生成：2026-09-23 · WorkBuddy（MacBook Air）
> 用法：整段复制，连材料一起发给审查者。

---

你上一轮交付了《External Skill Intelligence v2.4 项目相关性最终收口提示词 V1.1》（共 14 节）。现已按其要求完成整改，请复审。

**本轮范围声明**：v2.3 已验收通过的来源层、安全架构、Top 质量门、Canonical 体系、功能族折叠、归档自定位**全部未重做、未回退**（§十二）。本轮只修三类问题：**项目相关性假证据 / testing_qa 误标 / domain_mismatch 不生效**。

## 一、材料

- 文件名：`转送审查者-v5-外部情报层-2026-09-22.zip`
- 大小 / 校验：**523,945 B（0.50 MB）· 24 个文件**
  SHA-256 `dce8796dc8a85a2a1ca4b6dd37e3dd54897e6685319727647a4e145adb4fc37c`
- 解压后顶层文件夹：`转送审查者 v5 材料（外部情报层）/`
- 上一包（v2.3）已改名为 `…-v2.3-旧包.zip`，本包为**替换件**，请以本包为准。

解开后能看到什么：

| 文件 | 看什么 |
|---|---|
| `转送说明.md` | 给审查者的导读（9 节），建议先看 |
| `README.md` | 技术说明；**§3.1** 是 §一/§二/§十 的落地记录，**§6.3.2** 是 §三–§八 的落地记录，**§6.4** 是 §九 的落地记录 |
| `CANDIDATES_DIGEST.md` | 精简对照件；**§五** 是 v2.4 整改记录表（含 5.1/5.2），**§一** 是 `PROJECT_MATCH_QUALITY` 分项表，**§2.1** 是功能族 ALTERNATIVES 表 |
| `EXTERNAL_SKILLS_CONTEXT.md` | 日报底座；含独立的 **`PROJECT_MATCH_QUALITY`** 机器块与 `PERSONALIZED_TOP30` 自证字段 |
| `SKILL_CANDIDATES.json` | 全量候选池 1,106 条（任何数字都可自行复算）；顶层带 `project_match_quality` |
| `SKILL_SOURCE_REGISTRY.json` | 来源注册表 17 源 |
| `支撑/` | scripts 10 个 + `config/scoring.json` + `tests/test_pipeline.py`（**35 项**）+ 输入 2 个 |
| `raw/` | 当日原始发现快照 4 个 |

## 二、v2.4 逐节落地情况

| 节 | 你的要求 | 落地方式 | 可自查点 |
|---|---|---|---|
| §一 / §二 | 技术栈命中不得天然等于直接项目证据；拆 STRONG / SECONDARY | `TECH_MATCH` 每条加 `tier`。strong（Expo / RN / Android / Kotlin / Supabase / Postgres / Next.js / PWA / 离线本地存储 / macOS 桌面 / 原生 / LLM API / Playwright / Vercel / Netlify / Cloudflare / CF Pages / .NET / C#）可**独立**成立；secondary（TypeScript / React / Python / Docker / 自托管 / Tailwind / shadcn / 单文件 HTML / Shell）只加 `SECONDARY_BOOST = 3.0`，**绝不单独成立**。证据首项 `tech_stack` → **`strong_tech`** | `azure-microsoft-playwright-testing-ts` 的 `matched_projects` = **[]** |
| §二（补） | — | 加不变式守卫 `STRONG_TIER_MIN_W = 10` + 回归断言：每个 strong 技术栈的权重都必须够独立过 `min_score`。**排查中发现 Next.js 原为 9 分（归一化 18 < 20）→ 永远无法独立成立**，CF Pages / .NET / C# / 原生 同样被挡死，本轮一并修掉 | `test_v24_strong_tier_can_stand_alone` |
| §三 | Docker 词典删掉裸 `container` | Docker 只认 docker / dockerfile / docker compose / containerization / container image / container runtime / docker container / 容器化 | `responsive-design` 不再命中 Docker；`orca-per-workspace-env` 不再凭 container 匹配 |
| §四 | LEXICAL 收紧，泛词 stop list | `_LEXICAL_STOP` 扩到 manager / management / resource / service(s) / application(s) / system(s) / development / developer / testing / test(s) / data / model(s) / client / server / api(s) / process…；新增 `_LEXICAL_HIGH_SIGNAL`（prompt / insurance / rss / transcription / whisper / obsidian / valuation / dividend / playwright / supabase…）。判定：**1 个高信号词** → `min(14, 10+2n)`（够单独过 min_score）；**≥2 个 generic（≥5 字符）** → `min(10, 6+2n)`；**单泛词** → 不成立并计入 `lexical_single_rejected` | `manager` 单独不再匹配 `prompt-manager` |
| §五 | NEED_RULE 加否定语境 | 新增**通用否定窗口**（非 Playwright 补丁）：`_CLAUSE_SPLIT_RE` 按小句切分（句点**后必须跟空白/行尾**才算分句，保护 `next.js` / `.net` / `node.js`）+ `_NEG_MARK_RE`（not / never / don't / does not / avoid / without / not intended / 非用于 / 不用于 / 不支持 / 不适用 / 禁止 / 不要 / 不可）+ `strip_negated()` + `hit_pos()`。判定只在**正向视图**做；**Skill 名不受描述否定影响**；被压制且原本会命中的记入 `negated_evidence_rejected`。`MATCH_RULE` 同步新增 `any_groups`（组内 AND / 组间 OR）与 `custom` | `azure-resource-manager-playwright-dotnet` 不再命中 `browser-qa` |
| §六 | deploy 继续收紧 | 删正向 `any_of` 里的裸 `hosted`；`hosting / hosted / publish / publishing` 必须与**部署对象**（app / application / site / service / pages / build / artifact / production / environment…）**同现**（`any_groups`）才成立；CI/CD 只作 context | `azure-microsoft-playwright-testing-ts` 不再命中 `deploy` |
| §七 / §八 | testing_qa 改「主能力证据」 | `CAP_RULE["testing_qa"] = {"custom": _rule_testing_qa}`。**A.** Skill 名含 test / tests / testing / qa / e2e / playwright / pytest / vitest / jest / cypress / spec / selenium；**B.** 描述把测试当核心任务（run / write / create tests、test application、QA workflow、acceptance / regression testing、validate behavior、test suite、e2e / unit / integration tests、browser testing、visual regression、testing standards、运行测试、编写测试、测试套件、自动化测试、端到端测试…）。`testing methodologies` / `optional QA` / `may be tested` / `QA is optional` / `examples include testing` 只算**提及** | 3 条误标→不再得；`webapp-testing` / `e2e-testing-patterns` 仍得（反向保护） |
| §九 | domain_mismatch 必须在 Top 真正起作用 | 新增 `MISMATCH_DOMAIN` / `mismatch_domains()` / `project_domains()`；`evaluate()` 计算 `domain_mismatch_resolved` / `_unresolved` / `_resolved_by`；`DOMAIN_MISMATCH_PENALTY = 0.7` 总分成乘数（系数记在 `domain_mismatch_penalty`，可审计）；`recommend_of` 对 unresolved **最高 watch**；`select_top(..., require_no_mismatch=True)` 作为**第四道门**。**解除看的是整个项目档案的技术面**（不是「该候选恰好匹配到的项目」，否则会变成事实上的永久 blacklist，§九 明确禁止） | Top 内 `top_candidates_with_unresolved_mismatch` = **0**；`mismatch_blocked_from_top` = 2 |
| §十 | 精度指标换成 PROJECT_MATCH_QUALITY | `match_rules.QUALITY` 计数器 + `reset_quality()`；`analyze.main()` 在最后一次 `evaluate()` 前清零，写进 `out["project_match_quality"]` 并打印 `PROJECT_MATCH_QUALITY | ...`；`build_context` 输出同名机器块。**「字段非空率」不再作为精度指标** | 下表 §三 |
| §十一 | 新增 18 项回归测试 | 新增 **8 个测试函数**覆盖 18 个回归点 + 命中原语修复：`test_v24_strong_tier_can_stand_alone` / `test_v24_tech_evidence_tiers` / `test_v24_negation_window_and_deploy` / `test_v24_matching_primitives` / `test_v24_lexical_stoplist` / `test_v24_testing_qa_primary_capability` / `test_v24_domain_mismatch_gate` / `test_v24_project_match_quality`。测试项 **27 → 35** | `pass=35 skip=0 fail=0` |
| §十三 | 重新生成 v2.4 基线 | registry / candidates / context / digest / README 全部按 v2.4 口径重生成，未保留旧数字 | — |

### 附带修掉的两个「规则看起来对、实际不生效」缺陷（排查中发现，已如实登记）

| # | 缺陷 | 后果 | 处置 |
|---|---|---|---|
| **A** | 中文关键词命中率**恒为 0** | `_WORD_RE` 是纯 ASCII（`[a-z0-9]…`），而中文没有词边界 → 规则表里 **48 个 NEED_RULE + 27 个 CAP_RULE 中文关键词**，加上 `TECH_MATCH` 的 `容器化` / `菜单栏应用`，**全部是永不命中的死词**；§五 专门列的中文否定标记（非用于 / 不用于 / 不支持）也因此形同虚设 | 含 CJK 的关键词改走子串匹配；回归测试对规则表做**全量自检**（中文关键词不得再有死词），同时保留英文整词匹配（`"ui" in "build"` 的老事故不许回归） |
| **B** | 分句不含逗号 → 否定窗口**过度压制** | 「非用于 A，可用于 B」整句被丢弃，把逗号后的**正向证据**一起误杀 | 逗号（`,` / `，`）纳入分句边界，否定只作用于本小句；修掉后 `negated_evidence_rejected` 由 **94 → 30**，即删掉了 **64 处「假压制」** |

## 三、我方自测验收摘要（§十四 规定格式）

```text
PROJECT_MATCH:
false_positive_regression: PASS
matched_candidates: 106
strong_tech_evidence: 100
positioning_evidence: 11
shared_need_evidence: 28
lexical_evidence: 49
secondary_only_rejected: 102
negated_evidence_rejected: 30

CAPABILITY_TAGS:
testing_qa_false_positive_regression: PASS

DOMAIN_MISMATCH:
top_candidates_with_unresolved_mismatch: 0

TOP:
target_limit: 30
actual_count: 20
functional_duplicates: 1
contains_ignore_or_reject: no

SECURITY:
regression: PASS

TESTS:
pass: 35
skip: 0
fail: 0

BLOCKERS:
none
```

（补充口径：`lexical_single_rejected = 887`、`matched_project_entries = 167`、`unresolved_mismatch_candidates = 311`（全池，只作观察）、`t3_uncorroborated = 23`、`mismatch_blocked_from_top = 2`、安全 pass 396 / review_required 201 / block 3 / unscanned 506。）

## 四、请你复核这五点

1. **技术栈分级是否真的生效**（`支撑/scripts/match_rules.py` → `TECH_MATCH` 的 `tier`、`match_projects`）。
   建议直接验三条：① 泛用技术栈（TypeScript / React / Python / Docker…）**单独**不得产生 `matched_projects`；
   ② `azure-microsoft-playwright-testing-ts` 的 `matched_projects` 应为**空**；
   ③ `responsive-design` 的 `container queries` **不得**命中 Docker。
   再随机抽几条 `matched_projects` 非空的候选，逐条看 `direct_evidence_kinds` —— **只有 `strong_tech` 才算强证据**。
2. **否定窗口是否真的通用**（`strip_negated` / `hit_pos` / `_note_suppressed`）。
   验两点：① `azure-resource-manager-playwright-dotnet` **不得**命中 `browser-qa`；
   ② 自己造一句 `This tool does not run tests.` → 该关键词不得作为正向证据。
   同时验**反向保护**：`Next.js App Router patterns.` 里的 `next.js` 不得被句点规则切碎误压。
3. **平台错配是否既"挡得住"又"解得开"**（`analyze.py` → `evaluate` / `recommend_of` / `DOMAIN_MISMATCH_PENALTY`）。
   验两点：① 未解除错配的候选不得是 `install_candidate`（全池 311 条带错配，Top 内 **0** 条）；
   ② **解除路径必须真的通**——拿一个 `domain_mismatch = azure` 的候选，喂一个"使用 Azure 的项目"进去，
   `domain_mismatch_unresolved` 应变空、`domain_mismatch_penalty` 应消失、应恢复参与排序。
   请注意我们刻意看的是**整个项目档案的技术面**，而不是"该候选恰好匹配到的项目"——
   后者会让没有别的直接证据的 Azure 候选永远无法解除，变成事实上的永久 blacklist（§九 明确禁止）。
4. **Top 的四道质量门**（`build_context.py` → `select_top`）：① `score ≥ 55`；② 有个性化证据；
   ③ 无 T3 无佐证条目；④ **Top 内不得有任何未解除平台错配**（自证字段应为 0）。
   另核对同一功能族只出一条 PRIMARY，其余能在 `### 2.1 ALTERNATIVES` 里查到。
5. **归档包自定位与摘要诚实性**：解压后直接跑 `python3 支撑/tests/test_pipeline.py`。
   **只有本包**（无同级 v4 文件夹）时预期 `pass=34 skip=1 fail=0`（35 项；输入 A 自动命中包内文件，输入 B 找不到 → 1 项 SKIP 并打印原因，**不计入 PASS**）；
   把 `转送审查者 v4 材料（Canonical 模型）` 放到同级后再跑，预期 `pass=35 skip=0 fail=0`。
   对照件可复现性：`python3 支撑/scripts/make_digest.py -o /tmp/x.md`，除「自定位来源」那一行外，应与包内 `CANDIDATES_DIGEST.md` 逐位一致。

## 五、输出要求

只回：一段结论（通过 / 不通过 + 理由）+ 如有不符合处，按同一格式给出下一轮整改提示词。不要复述本提示词。
