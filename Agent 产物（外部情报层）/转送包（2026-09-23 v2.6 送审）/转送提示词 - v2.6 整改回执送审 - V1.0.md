# 转送提示词 —— External Skill Intelligence v2.6 整改回执送审（冻结前完整性修正）

> 生成：2026-09-23 · WorkBuddy（MacBook Air）
> 用法：整段复制，连材料一起发给审查者。

---

你上一轮交付了《External Skill Intelligence v2.6 冻结前身份血缘与语义完整性整改提示词 V1.1》（共 15 节）。现已按其要求完成整改，请复审。

**本轮范围声明**：v2.5 的来源分层、安全 Gate、Deep Scan、Project Match 强弱证据、否定窗口、Top 质量门、Functional Family、归档自定位**全部未重做、未回退**（§十四）。本轮只修你列出的 7 类复审问题，**未新增任何 blacklist**——全部改的是通用证据规则。全程未触碰 v4 冻结底座 / Skill Manager / 已装 Skill，未安装任何 Skill。

## 一、材料

- 文件名：`转送审查者-v5-外部情报层-2026-09-23-v2.6.zip`
- 大小 / 校验：**596,699 B（0.57 MB）· 24 个文件**
  SHA-256 `1d37f16b6e5af213b92d4d1cd24cc2f47699a9be1636a979de09daf096ebc13d`
- 中文名 UTF-8 标志位 **24/24**（Python zipfile 打包，实测复核）
- 解压后顶层文件夹：`转送审查者 v5 材料（外部情报层）/`
- 上一包（v2.5 送审件）为**被替换件**，请以本包为准。
- **归档包端到端已实测**（`/tmp` 隔离目录）：解压直跑 `51 pass + 1 SKIP`（缺 v4 底座，SKIP 打印原因不计 PASS）；v4 放同级后 `52/52`；对照件重生成与包内件**只有 1 行环境相关差异**（输入 A 自定位行），其余逐位一致。

解开后能看到什么：

| 文件 | 看什么 |
|---|---|
| `转送说明.md` | 给审查者的导读（9 节），建议先看；§四 是 v2.6 基线数字（含三处刻意变化的解释） |
| `CANDIDATES_DIGEST.md` | 精简对照件；**§五 是 v2.6 七类问题整改表**；**§4.0 UPDATE_LINEAGE 血缘核对表**；**§5.4 是按包内真实数据现算的十项断言表**（不是手写结论） |
| `EXTERNAL_SKILLS_CONTEXT.md` | 日报底座，机器区扩为 **12 节**（新增 `UPDATE_LINEAGE`）；Top 自证字段含 `top_candidates_with_unresolved_product_scope` |
| `README.md` | 技术说明；**§6.3.4 是 v2.6 §二–§十 的落地记录（a–e）**；§3.1 质量计数表现在有 **5 个恒 0 哨兵** |
| `SKILL_CANDIDATES.json` | 全量候选池 1,106 条（**version 6**）；每候选新增 `lineage_evidence` / `update_lineage` / `scope_type` / `target_product` |
| `SKILL_SOURCE_REGISTRY.json` | 来源注册表 17 源（来源层未动） |
| `支撑/` | scripts 10 个 + `config/scoring.json`（**version 保持 4：未动任何权重**）+ `tests/test_pipeline.py`（**52 项**）+ 输入 2 个 |
| `raw/` | 09-22 / 09-23 两轮发现快照 + 缺口焦点 |

## 二、v2.6 逐节落地情况

| 节 | 你的要求 | 落地方式 | 可自查点 |
|---|---|---|---|
| §二 / §十一 | 身份按血缘不按同名 | `installed_relationship()` 的同名分支不再直返 `already_installed`：必须过 `lineage_confirmed`（① normalized upstream 一致；② 候选 owner/repo 与已装 upstream 明确一致；③ 内容 fingerprint；④ Source Map 记载上游/内容同源；⑤ 显式 `LINEAGE_ALIASES` 表——leader、neat-freak 依 Source Map『逐字节一致』证据入表）。不过 → `same_name_unverified`（已装侧 upstream=unknown，4 条）/ `same_name_different_source`（上游记录不同，2 条）：不标已装、不建 update/replacement、`recommend_of` 最高 watch、NOVELTY=0、gap 封顶 5。全码清扫：replacement 的 known_missing 同名查找也过血缘门；名称只参与 similarity | 池内 `already_installed` 8 条**全部**带 `lineage_evidence`；`same_name_only_already_installed = 0`（哨兵） |
| §三 | update 与同名匹配彻底分开，读 Source Map | 新增 `build_update_lineage(cands, inst)`（日报与对照件共用）：更新项由 `SKILL_SOURCE_MAP.json` 的 version_status=outdated/historical + upstream_activity=active 驱动，逐条输出 `installed_canonical_id / installed_upstream / update_upstream / lineage_evidence / lineage_verified`；不一致必须有血缘；**池内无同血缘候选时更新来源直接取 Source Map 的 upstream，不绑 repo** | `UPDATE_LINEAGE` 3 条：aihot → **Virxact / AI HOT**（不再绑 Khazix）、browseros-neo → browseros-ai/BrowserOS（血缘一致，候选侧 update_available=true ✓）、computer-use-2 → stablyai/orca |
| §四 | GCP 产品别名 + product scope 进推荐 Gate | `MISMATCH_KW/MISMATCH_DOMAIN` 补 14 个 GCP 产品别名（cloud run / agent platform / model garden / vertex ai / vertex / cloud build / cloud functions / firestore / gke / google kubernetes engine / cloud sql / alloydb / cloud monitoring / cloud logging）→ 全部 `gcp`；**裸 Gemini 不入词典**；另立 `microsoft-store` 域（msstore-cli 81 → 45.4 watch，Windows 设备 ≠ Store 发布需求）；哨兵 `gcp_alias_missed` | cloud-run-basics / agent-platform-deploy / agent-platform-endpoint-management 均 `domain_mismatch_domains=[gcp]`，**全部离开 Top**（两条降 ignore、endpoint-management 因 §八 失去 deploy 需求后降 ignore）；`gcp_alias_missed = 0` |
| §五 | 跨 Skill 重定向句不作本 Skill 能力 | `_REDIRECT_RE` + `strip_redirects()`：识别 For X, use/see Y（逗号可有可无）· use Y instead · handled/covered by Y · 如果是X请使用Y / X请改用Y；**目标 Y 必须是「另一个 Skill」形态**（连字符 slug 或 `the X skill`；`use this skill`、`Use when users want to create a skill` 不误剪）。`_ctx` 先剪重定向（原始句界还在时）再剪否定；计数 `redirect_evidence_rejected=126`。连带修三处「规则看起来对、实际不生效」：`fm_description` 300→**600**（截断砍掉重定向句尾）、`match_projects` ①② 从原始文本改到**正向视图**、TECH Android 去裸 `emulator` + `_QA_OR_DEVICE` 补 `simulator` | `orca-emulator`（iOS）：needs=[] / MPs=[] / 权重 27 消失 / install_candidate→watch 50；mobile_qa **保留**；`orca-emulator-android` 反向保护：android+mobile_qa、79.4、Top #1 |
| §六 | 使用 MCP ≠ 开发 MCP | `_MCP_ARTIFACT_RE`（mcp/model context protocol + server/client/tool/integration/protocol/sdk…）+ 60 字符窗口内必须有 `_MCP_DEV_VERB_RE`、且工件紧贴 using/via/through/by 判为使用；`_rule_mcp_dev` **只认 primary**；使用语境 → 新增独立标签 `mcp_usage`（不吃 mcp_dev 缺口分） | `penpot-uiux-design`：mcp_dev ✗、mcp_usage ✓、frontend_design ✓；`github-issues using MCP` 型不命中；`mcp-builder` / `php-mcp-server-generator` / `rust-mcp-server-generator` 均 primary ✓；`MCP_USAGE_AS_MCP_DEV = 0` |
| §七 | frontend_design 收紧 + image_creative 扩充 | CAP+NEED 同步：删裸 frontend / front-end / ux / css / tailwind / shadcn / visual design，只认明确设计语义（ui design / web design / design system / visual hierarchy / typography / responsive design / styling / css design / component design / interface design / frontend design / design review / 界面设计·前端设计·设计审查）；image_creative 词表扩充（generate/create images、artwork、visual art、poster、illustration、sprite、texture、visual asset、static visual design、image mockup、图像生成/图片生成/海报/插画/图标生成/视觉素材…），静态视觉创作优先归 image_creative | `webapp-testing` 不再得 frontend-design（browser-qa+testing_qa 保住）；`canvas-design` / `generate-image` → **image_creative 命中 `none` 真缺口**（generate-image 84.5 进 install_candidate 路径）；真设计三件（web-design-reviewer / frontend-design-review / tailwind-design-system）保住 ✓ |
| §八 | deployment 名词 ≠ 部署能力 | `DEPLOY_RULE.any_of` 删裸 `deployment`；primary = deploy/deploying+对象 / publish+app/site/artifact / *pipeline / rollout / deploy to X / provision deployment / 部署应用·发布站点·上线服务；裸名词只能 mention | `e2e-testing-patterns`、`agent-owasp-compliance` 的 deploy 需求消失（testing_qa/security_audit 保住）；`deployment categories` / `Ease of Deployment` / `deployment considerations` 全不命中；cloud-run-basics 的「deploy applications」仍是合法 primary（由 §四 gcp 域挡 Top） |
| §九 | spec/specs 歧义 | `_TESTING_NAME_TOKENS` 删裸 `spec`/`specs`、补 `rspec`；`_TESTING_CORE_PHRASES` 加 test spec / test specification / spec test / executable specification / 测试规格·测试用例 | `gen-specs-as-issues`：testing_qa ✗、spec-driven ✓；`test-spec-generator` / `rspec-*` ✓ |
| §十 | 产品内部 Skill 与通用 Skill 区分 | `product_scope()`：`scope_type` ∈ generic / platform_operation / product_internal + `target_product`；product_internal 判定**以候选来源仓库即产品仓为前提**（自研动词 + 产品名 + app/extension/ui/codebase 句式）——「Build apps on Supabase」不误伤；`product_internal_unresolved()`：项目档案真的在用/开发该产品才解除；`recommend_of` 未解除 → 最高 watch；`select_top` **第五道门** `require_no_unresolved_scope`。**不是 blacklist，可解除** | BrowserOS `test-ui`：71.3 install_candidate → **watch、不进 Top**；池内 product_internal 2 条（均未解除）；`top_candidates_with_unresolved_product_scope = 0`；喂「开发 BrowserOS 的项目」测试证明解除路径通（`test_v26_product_internal_gate`） |
| §十二 | 20 项数据级回归 | 新增 **9 个测试函数**逐项覆盖：`test_v26_lineage_identity`（1,2,3,5）/ `test_v26_update_lineage_data`（4,20）/ `test_v26_gcp_aliases_and_scope`（14,15,16,17,19）/ `test_v26_product_internal_gate`（18）/ `test_v26_redirect_not_capability`（4,5）/ `test_v26_mcp_usage_vs_dev`（6,7）/ `test_v26_frontend_design_and_image_creative`（8,9,10）/ `test_v26_deploy_noun_and_spec_tokens`（11,12,13）/ `test_v26_final_data_assertions`（§十三 全 10 项）。测试 **43 → 52** 项 | `pass=52 skip=0 fail=0`；隔离解包 `51 + 1 SKIP` |
| §十三 | 重算与十项数据断言 | 断言全部实现为可复算（analyze 输出 + `test_v26_final_data_assertions` + 对照件 §5.4 现算表） | 10 项全 0，见下表 |
| §十四 | 不回退 v2.5 | 原 43 项全保留通过。**一处按你 §十四 精神做的口径更新**：描述窗口 300→600 后 `react-view-transitions → yejian-buguangdeng` 以 `strong_tech: Next.js` 合法重现（项目技术栈确含 Next.js，属你要求保留的强证据命中）；v2.5 禁的 **react/native/裸 prompt 词面依据** 仍被两个恒 0 哨兵禁死，旧禁选测试相应改为**按依据类型断言**（README §十一 v2.6 条目与对照件 §5.2 均如实记录） | `test_v25_lexical_evidence_tightened` / `test_v25_top_reaudit_final_data` 更新后仍绿；claude-api / breakdown-test → prompt-manager 两对**依然为 0** |

## 三、我方自测验收摘要（§十五 规定格式，交付前现跑）

```text
IDENTITY:
same_name_only_already_installed: 0
wrong_update_lineage: 0
update_candidates_lineage_verified: yes

SEMANTIC_REGRESSION:
android_redirect: PASS
mcp_dev: PASS
frontend_design: PASS
image_creative: PASS
deploy: PASS
testing_qa_specs: PASS

SCOPE:
gcp_alias_missed: 0
top_unresolved_product_scope: 0
product_internal_gate: PASS

TOP:
target_limit: 30
actual_count: 18
contains_ignore_or_reject: no
unresolved_domain_mismatch: 0
unresolved_product_scope: 0

TESTS:
pass: 52
skip: 0
fail: 0

BLOCKERS:
none
```

（补充口径：§十三 十项断言全 0——WRONG_UPDATE_LINEAGE / SAME_NAME_ONLY_ALREADY_INSTALLED /
TOP_UNRESOLVED_PRODUCT_SCOPE / GCP_ALIAS_MISSED / ANDROID_REDIRECT_FALSE_POSITIVE /
MCP_USAGE_AS_MCP_DEV / FRONTEND_TEST_AS_FRONTEND_DESIGN / IMAGE_CREATIVE_KNOWN_FALSE_NEGATIVE /
DEPLOY_OUTCOME_AS_DEPLOY_CAPABILITY / PRODUCT_SPEC_AS_TESTING_QA。
池 1,106 / version 6；install_candidate 12 / watch 454 / ignore 635 / reject **5**——
其中 genkit dart/go 两条 block 是 600 抓取扫描面轮换后按既有规则命中真实 `curl … | bash`，
**安全规则层零改动**，属 fail-closed 方向，名单请见对照件 §3.1。
PROJECT_MATCH_QUALITY：matched_candidates 76 / strong_tech 119 / positioning 16 / shared_need 30 /
lexical 2；redirect_evidence_rejected 126；五个恒 0 哨兵全 0；覆盖率 22/107、完整性 146/146。
`config/scoring.json` version 保持 4。）

## 四、请你复核这五点

1. **血缘模型的兜底强度**：① 全池 `already_installed` 是否**每一条**都有非空 `lineage_evidence`；
   ② 6 条 same_name_* 是否确实没有 update/replacement 关联且最高 watch；
   ③ `LINEAGE_ALIASES` 只有 leader / neat-freak 两条且都附 Source Map 逐字节一致证据——
   请核对这种「有证据才入表、没证据就 unverified」的保守姿势是否接受（storage-analyzer /
   find-skills / deploy-to-vercel / make-repo-contribution 因证据缺失**没有**被放进别名表）。
2. **UPDATE_LINEAGE 是否真的换成了 Source Map 驱动**：`build_update_lineage()` 三条更新的
   upstream 与依据；候选侧 `update_available` 仅剩 browseros-neo 一条（血缘一致）。
   若你认为三条中某条血缘不该成立，请指出证据。
3. **第五道门与「可解除」承诺**：给一个「开发 BrowserOS」的项目档案复跑 analyze，
   `product_internal_unresolved` 应变空、恢复参与 Top——**验证它不是变相 blacklist**。
   同时验误伤保护：「Build apps on Supabase」不得被判 product_internal。
4. **重定向窗口的边界**：自造句子验三件事——① `For X, use other-skill` 剪；
   ② `Use when users want to create a skill …` 不剪；③ `use this skill` 不剪。
   再核 `fm_description` 300→600 的影响面：reject 3→5 与 MP entries 122→146 都源自
   「读到了更多真实文本」，请抽查新增 entries 的证据类型（应无 lexical）。
5. **react-view-transitions 的口径更新是否可接受**（本包唯一改动过的旧断言）：
   我们**没有**放宽 v2.5 对词面依据的禁令（哨兵仍 0、claude-api/breakdown-test 两对仍 0），
   只把「该配对完全不得出现」改成「不得以 lexical 依据出现」。
   若你认为 Next.js strong_tech 也不该匹配该项目，请给出项目技术栈侧的判定依据。

## 五、输出要求

只回：一段结论（通过 / 不通过 + 理由）+ 如有不符合处，按同一格式给出下一轮整改提示词。不要复述本提示词。
