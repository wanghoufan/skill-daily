# 转送提示词 —— External Skill Intelligence v2.7 整改回执送审（冻结前一致性与 Top 精度修复）

> 生成：2026-09-23 · WorkBuddy（MacBook Air）
> 用法：整段复制，连材料一起发给审查者。

---

你上一轮交付了《External Skill Intelligence v2.7 冻结前一致性与 Top 精度修复提示词 V1.1》（共 14 节）。现已按其要求完成整改，请复审。

**先回应你 §一 的核心指控**：v2.6 归档包「本机 52/52、审查环境 50/1/1」**已查明为两个真实缺陷**并已修复——① `_ctx` 用 `' '.join(set)` 重建 Skill 名，分类随 PYTHONHASHSEED 翻转（`php-mcp-server-generator` 即你抓到的那一条）；② `scan_budget=600` 按 preliminary 排序分配，语义变化后部分候选（含 wiki-qa、python-appservice-deploy）落在预算外、**description 为空**，判定基于空文本失真。二者在 v2.7 都修成了**可复现**，并把「确定性」升级为测试硬门（§十一）。同时你 §三–§八 点名的 Top 假相关全部按通用规则修复，**未新增任何 blacklist**；v2.6 已验收机制**全部保持**（§十二）。

## 一、材料

- 文件名：`转送审查者-v5-外部情报层-2026-09-23-v2.7.zip`
- 大小 / 校验：**713,982 B（0.68 MB）· 24 个文件**
  SHA-256 `bae63c85f9d0acc702bbf364995c32550397c1265bfb459af5e249c6ca8ec7a2`
- 中文名 UTF-8 标志位 **24/24**（Python zipfile 打包，实测复核）
- 解压后顶层文件夹：`转送审查者 v5 材料（外部情报层）/`；上一包（v2.6）为被替换件。
- **归档包端到端隔离实测（本轮，`/tmp`，非主仓路径）**：
  解压直跑 → `pass=57 skip=1 fail=0`（缺 v4 底座那 1 项 SKIP 打印原因，不计 PASS）；
  v4 放同级 → `pass=58 skip=0 fail=0`；
  **`PYTHONHASHSEED=7` 复跑 → 同样 57/1/0，逐字节可复现**；
  矩阵门 `test_v27_determinism_hashseed` 在包内自带（seed 0/1/2/3/42/123 子进程，输出不一致即 FAIL）。
  对照件重生成与包内件仅 1 行环境相关差异（输入 A 自定位行），其余逐位一致。

解开后能看到什么：

| 文件 | 看什么 |
|---|---|
| `转送说明.md` | 导读（9 节）。**§四 表格下方有「为什么数字不能与 v2.6 直接同比」的三条注记**（scan 覆盖扩大 / negated 口径变化 / entries 回升），请先读再核数 |
| `CANDIDATES_DIGEST.md` | **§五 = v2.7 七行整改表**（含第 7 行「扫描预算诚实化」，是我们主动多修的一处）；**§5.5 = 数据级断言现算表**（v2.6 十项 + v2.7 六案 + KNOWN_FALSE_POSITIVE_COUNT + DETERMINISM 行）；§4.0 UPDATE_LINEAGE；§3.1 block 9 条名单 |
| `EXTERNAL_SKILLS_CONTEXT.md` | 机器区 12 节；PROJECT_MATCH_QUALITY 含 `product_specific_scope_unresolved` 与 determinism 行；Top 自证含 `scope_blocked_from_top: 14` |
| `README.md` | **§6.3.5 = v2.7 §二–§八 落地记录（a–g）**；§6.3.4（v2.6）与 §3.1 保留；已知限制含 v2.7 预算条目；§十一变更日志 |
| `SKILL_CANDIDATES.json` | 池 1,106 条，**version 7**；因预算覆盖全池，几乎每条都有真实 description（这正是两边环境结果曾不一致的显影剂） |
| `支撑/` | scripts 10 个 + `config/scoring.json`（**version 保持 4，权重未动**；唯一数值变化 `scan_budget` 600→1200，字段注释写明理由）+ `tests/test_pipeline.py`（**58 项**）+ 输入 2 个 |

## 二、v2.7 逐节落地情况

| 节 | 你的要求 | 落地方式 | 可自查点 |
|---|---|---|---|
| §二 | MCP 分类非确定性：禁止从 set 回拼名称 | `_ctx` 保存 **`raw_name` / `name_norm`（按 `-_/` 稳定规范化的有序串）/ `name_words_set`（仅 membership）**；`pnorm` 追加有序名；`_cls_mcp` 的 nn 改读 `name_norm`。全码清扫：名称短语 / MCP 名称断言 / testing 身份词均不再触碰 set 顺序 | 包内跑矩阵即证；或自己 `PYTHONHASHSEED=任意 python3 支撑/tests/test_pipeline.py` 复现 |
| §二（回归） | 6 seed 输出完全一致，预期 builder/php/rust → mcp_dev、penpot → mcp_usage | 新增硬门 `test_v27_determinism_hashseed`（子进程矩阵 + 逐字节比对，「以后任何随机 seed 改变分类都必须 FAIL」） | 矩阵 6/6 一致；验收摘要 `classification_drift: 0` |
| §三 | `qa` ≠ Quality Assurance | 名称证据仅裸 `qa` 时必须有描述质保证据（`_QA_REAL_TERMS`）；`_QA_NEG_PHRASES`（answers questions / q&a / knowledge qa / document qa / 问答…）在无其他测试身份时**一票否决**；新增独立标签 `question_answering`（不吃 testing_qa 分） | 数据里 `microsoft/skills/wiki-qa`：capability_tags 无 testing_qa ✓、有 question_answering ✓、不在 Top ✓。反向：`webapp-testing`、合成 `place-journal-qa`（真验收描述）仍命中（`test_v27_qa_ambiguity`） |
| §四 | deploy 必须绑定真实动作+对象；rules/policies/games/content 等不算 | 删「动词×对象全文任意共现」，改 `_DEPLOY_PROX_RE`：**动词与通用对象同句 ≤50 字符邻近**；对象白名单（app/site/service/function/container/workload/artifact/pages/production…）；`_DEPLOY_INTERNAL_OBJS`（rules/policies/prompts/detections/configs/dashboards/alerts/model settings/games/content/documentation）不进白名单；另认 `deploy to <云/vercel…>` 与 pipeline/roll out/中文词组 | `game-engine` needs 无 deploy（并退出 Top）✓；`detection-engineering` 的「deploy YARA-L rules to SecOps」不成立 ✓；`deploy-to-vercel` / `python-appservice-deploy` / `Deploy the app to production.` / `Static hosting for your site` 保住 ✓（`test_v27_deploy_proximity` + §四回归清单逐条） |
| §五 | `breakout` 撤出独立短语表，建 `AMBIGUOUS_DOMAIN_TERMS` | pepe 词条改为 trading/price/market breakout、breakout radar/signal、technical analysis、backtest、量化交易等；新增 `_AMBIGUOUS_DOMAIN_TERMS`（breakout/native/model/agent/manager/dashboard/channel/store/platform/stream/shell/library/network）+ `_is_ambiguous_single()` 在 phrase 判定处强制执行「单词不得独立成词面证据」 | `game-engine → pepe-doge-breakout-radar` 消失 ✓；合成 `trading-breakout-strategy` 仍可匹配（反向）✓；`test_v27_lexical_domain_and_native_mask` |
| §六 | React Native ≠ native macOS/iOS/Android；最长短语优先+占位 | `_rn_masked()`：TECH_MATCH ① 判定前把 `react native / react-native / nativewind` 从正向视图遮蔽占位 | `react-native-design` 的 MPs 只剩 RN/Expo 项目、**DeepSeekBalanceWidget-Mac 消失** ✓；合成 Swift/AppKit 原生候选仍匹配该项目（反向）✓ |
| §七 | 用 platform_operation 做真正的产品专项门 | `_PRODUCT_OPERATION_TERMS`（google-analytics / google-secops / anthropic-brand / microsoft-store / shopify / salesforce / servicenow / databricks / snowflake）→ `scope_type=platform_operation + target_product`；`scope_unresolved()` 统一解除判定（项目档案按词典词组命中即解除；GCP/Azure 域仍走 mismatch 机器，不双计）；未解除 → 推荐最高 watch + **第五道 Top 门扩覆盖** | 数据：GA Admin / SecOps / brand-guidelines 三条均 `platform_operation` + 未解除 + 不在 Top；测试内置「喂一个 Google Analytics 项目 → 自动解除」的验证（非 blacklist 承诺可执行） |
| §八 | secret-safety 要求安全动作 | NEED_RULE 改 `all_groups[安全对象组] AND [安全动作组]`（store securely / rotate / scan / redact / mask / encrypt / vault / keychain / least privilege / prevent leakage / 密钥管理·轮换·凭据扫描·防泄露·脱敏…） | `google-analytics-admin-api-basics` 的 secret-safety 消失 ✓；`secret-scanning` / secrets-management / keychain 型保住 ✓（`test_v27_product_specific_scope` 断言） |
| §九 | Top 数据级六断言 + 三 generator 稳定 mcp_dev | 全部进 `test_v27_top_final_audit` + 对照件 §5.5 现算表 | 六条全 0 / `known_false_positive_count: 0`；三 generator 在 6 个 seed 下均 mcp_dev primary |
| §十 | 重审 Top18，修规则不拉黑 | Top 重算为 **20**（scan 补全后新入池的真实命中）；你点名的 6 条逐一按上表通用规则退出，**代码中不存在任何按 canonical_key 的例外名单** | 已逐处核对：这些名字在 `scripts/` 中只出现在**注释、整改说明表与 §5.5 审计断言**（只读数据、不产生判定）里；match_rules / analyze 的判定路径（规则、评分、推荐）不含任何按名的分支 |
| §十一 | 确定性成为正式门 + 报告含 DETERMINISM | 矩阵测试入常规套件（普通跑法即包含）；验收摘要首段即 DETERMINISM | 「本机 52/52、审查环境 50/1/1」类问题从机制上不可再现（矩阵 + 全池描述） |
| §十二 | 不回退 v2.6 | v2.6 的 52 项全部保留通过（其中 deploy 内部结构断言按 §四 新语义同步改写，断言意图不变：裸 hosted / CI/CD 仍出局、真部署仍命中）；aihot/skill-creator/orca redirect/T3/功能族/三分摘要等逐项在案 | 对照件 §5.1 不重做清单 + 58/58 |
| §十三 | 重生成 v2.7（不保数量） | registry / candidates(v7) / context / digest / README / tests 全部重出；Top 18→20、install 13、reject 5→9——**全部为现跑数字**，v2.6 旧数在 README §十一分条留档 | §三 下表 |

**我们主动多修的一处（§一「不一致」的第二根因，如实报告）**：`scan_budget` 600 按 preliminary 排序分配——语义一变，落选候选就没有 description，分类与 Top 随之失真且随环境漂移。已提到 **1200 覆盖全池**（实测 1,075 次抓取，raw.githubusercontent 不占 API 配额）。副作用入账：`unscanned 506→31`、`block 5→9`（新抓出的真 `curl | bash`，**安全规则零改动**，名单在对照件 §3.1）、`negated_evidence_rejected 253→54`（deploy 共现计数口径变化，不是门松）、`matched_project_entries 146→158`（描述补全的真实证据回升）。

## 三、我方自测验收摘要（§十四 规定格式，交付前现跑）

```text
DETERMINISM:
hashseed_matrix: PASS
classification_drift: 0

MCP:
mcp_dev_regression: PASS
php_mcp_server_generator: primary
rust_mcp_server_generator: primary
penpot_mcp_usage_only: PASS

TESTING_QA:
question_answering_false_positive: PASS
wiki_qa_testing_qa: no

DEPLOY:
game_engine_false_deploy: no
secops_rule_false_deploy: no
deploy_regression: PASS

PROJECT_MATCH:
breakout_cross_domain_false_match: no
react_native_to_native_macos_false_match: no

PRODUCT_SCOPE:
google_analytics_scope: platform_operation
google_secops_scope: platform_operation
anthropic_brand_scope: platform_operation
top_unresolved_product_scope: 0

SECRET_SAFETY:
product_secret_false_positive: PASS

TOP:
target_limit: 30
actual_count: 20
contains_ignore_or_reject: no
known_false_positive_count: 0

TESTS:
pass: 58
skip: 0
fail: 0

BLOCKERS:
none
```

## 四、请你复核这五点

1. **可复现性**（本轮的立身之本）：解压直跑应为 `57 pass + 1 SKIP`；放 v4 同级 `58/58`；
   任选 `PYTHONHASHSEED=7`（或任何值）复跑结果应逐字节稳定；矩阵门在包内。
2. **block 5→9 与 entries 回升是否接受为诚实结果**：全部源自「候选第一次被完整读到」，
   规则层零改动（`git` 不可查，但 `security_gate.py` 在 §八 边界声明中为未触碰文件，
   9 条 block 逐条可在 §3.1 表核对证据）。
3. **§四/§三 的反向保护**：真部署（deploy-to-vercel / appservice / 中文词组）、真 QA
   （place-journal-qa / webapp-testing）、真设计、真原生（Swift/AppKit）都没被收死——
   `test_v27_*` 六函数逐条断言，欢迎加句子反攻。
4. **scope 门的「可解除」承诺**：照 §十 指引给项目档案喂 Google Analytics 再跑 analyze，
   GA Admin 应恢复参排——测试 `test_v27_product_specific_scope` 已内置同型验证。
5. **v2.7 六处旧断言/口径变更的授权**：① deploy 内部结构断言随 §四 重写（意图不变）；
   ② v2.6 §5.2 react-view-transitions 的 lexical 依据禁令维持、strong_tech Next.js 允许；
   ③ `negated_evidence_rejected` 口径变化已在说明 §四注②入账。
   若你认为任何一处属于回退，请指名条款，我们按同一格式出下一轮。

## 五、输出要求

只回：一段结论（通过 / 不通过 + 理由）+ 如有不符合处，按同一格式给出下一轮整改提示词。不要复述本提示词。
