# 转送提示词｜Skill 日报引擎 V1.4（归档自定位与版本一致性收口）整改回执送审｜2026-09-24

对应整改文档：《Skill日报引擎V1.4归档自定位与版本一致性收口-提示词 - V1.1》（全 16 节）。
本轮未修改 External Skill Intelligence v2.11（`find -newer 上一轮回执` 为空，实测于打包后）；
未安装 / 删除 / 更新任何 Skill；V1.3 业务算法零改动（§十一）。

## 送审件

```
转送包（2026-09-24 日报引擎V1.4 送审）/
├── skill-daily-v1.4.zip   15 个文件
└── 本回执
```

zip SHA256（落笔前复测）：`6446c269c66bd3c951e0d4d1a824b8011689142e5dbd0fcb8fc08fa840771aa8`（96,696 B）

## 问题 1：归档自定位（§一~§六）

`scripts/common.py` 重写为集中式 `resolve_external_root()`：
优先级 ①EXTERNAL_INTELLIGENCE_ROOT ②同级 external-intelligence（开发仓）
③同级 转送审查者 v5 材料（外部情报层）④旧版 Agent 产物嵌套（继续兼容）；
每一级**验证** SKILL_CANDIDATES.json（或 data/ 下）与 build_context.py（或 支撑/scripts/ 下）
真实存在才采用；全部缺失 → 友好错误（含修复指引），不再 ModuleNotFoundError。
实际布局记入 `DAILY_REPORT_CONTEXT.external_resolution{mode, env_override_used}` 与
package-e2e-audit 顶部 `EXTERNAL_RESOLUTION:` 段。

新增 subprocess 级真 clean-room 测试（不是同进程 import 断言）：
t78 同级转送包无 env（全新 python 进程 import common+build_daily 成功且
mode=sibling_transfer_package）、t79 development_repo、t80 env_override、
t81 legacy_nested、t82 缺失时友好错误（断言输出无 ModuleNotFoundError 字样）。

## 问题 2：版本一致性（§八~§十）

当前版本标签 9 类位置统一 V1.4：README 标题 / build_daily 模块标题+ENGINE_VERSION 常量 /
argparse description / tests 套件标题 / config _comment / repeat-audit 标题 /
package-e2e-audit 标题 / lifecycle-e2e-audit 标题 / feedback-demo 标题 /
machine `engine:{name:"skill-daily", version:"1.4", auto_install:false}`。
历史说明（“V1.1 保留/V1.2 修复/V1.3 生命周期”）按 §九 保留在 docstring/README 历史段。
t83 CURRENT_VERSION_CONSISTENCY 逐位断言 + stale=0。

## §十三 用户级复现（亲自跑，已执行）

/tmp/skill-daily-final-review/ 全新目录，只做：
`unzip skill-daily-v1.4.zip` + `unzip 转送审查者-v5-...v2.11.zip`（两个顶层目录同级）→
`python3 skill-daily/tests/test_daily.py` → **73 pass / 2 skip / 0 fail**
（skip = t37 合成真 SKIP + t79 开发仓布局在此环境不适用；无任何 env/PYTHONPATH）。
随后 `build_daily.py --date 2026-09-24` → initial_baseline shown 10；
`--date 2026-09-25` → daily_delta shown 0。

## 业务零回退对照（§十一/§十二）

同级布局 E2E 与 V1.3 完全一致：Day1 10 条同 keys / Day2 0 / Day8 9 条全 cooldown_reminder；
lifecycle mutation 审计（真实池内存复制，External 文件零改动）：removed 通知一次、
install→reject 降级不静默、Day3 RETURNED 一次；功能重复 0、族配额 PASS、
绝对路径 grep 0、auto_install 0。

## §十五 最终验收摘要（全部数字为最终包实测）

```text
EXTERNAL_RESOLUTION:
sibling_transfer_package_no_env: PASS
development_repo: PASS
env_override: PASS
legacy_nested: PASS
friendly_missing_external_error: PASS

VERSION_CONSISTENCY:
engine_version: 1.4
current_version_consistency: PASS
stale_current_version_labels: 0

BUSINESS_REGRESSION:
day2_no_change_items: 0
removed_shown_candidate_notified: PASS
install_to_reject: PASS
returned_candidate_notified: PASS
replacement_candidate_supported: yes
functional_duplicates: 0
family_quota: PASS

CLEAN_ROOM:
exact_user_repro_command: PASS
env_override_used: no
package_report_matches_e2e: yes
absolute_dev_paths: 0

TESTS:
pass: 74
skip: 1
fail: 0
（开发仓 74/1/0；最终包用户级 clean-room 73/2/0，两处 skip 差为 t79 布局不适用，均按真 SKIP 计数）

QUALITY:
restore_misreported_as_new: no
metadata_misreported_as_update: no
no_longer_relevant_silent_loss: 0
status_downgrade_silent_loss: 0
auto_install_calls: 0

BLOCKERS:
none
```

## §十六 冻结条件逐条

1 同级双 unzip 直跑通过（实测 73/2/0）；2 不依赖 env/PYTHONPATH（final-review 断言 env 计数 0）；
3 友好错误（t82）；4 版本标签全 V1.4（t83）；5 V1.3 测试零回退（全套 + 业务对照）；
6 Day2=0；7 lifecycle E2E 零回退；8 绝对路径 0；9 auto_install 0；10 fail=0。
